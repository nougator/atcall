namespace Atcall

module Parser =
    let getInstructionByName (instruction: string[]) =
        match instruction.[0] with
            | "" -> ()
            | _ -> ()

    let AOT () = () // Ahead of time compilation